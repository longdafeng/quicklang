# Migration contract

V0001 is a proposed seekdb/OceanBase schema for the framework core.
It has not been executed against a real database.
Before integration: add checksum manifest, migration lock, forward-only runner,
backup-before-migration and tests against the pinned seekdb binary.
No migration automatically runs from the preview app.
